import React from "react";

const Header = () => {
  return (
    // we write some jsx into it
    <div className="ui fixed menu">
      {/* these classes are from semantic ui */}
      <div div className="ui container center">
        <h2>Fake Shop - Deepak</h2>
      </div>
    </div>
  );
};

export default Header;
